import React from 'react';
import { Container, Typography } from '@mui/material';

const ManagementScreen = () => (
  <Container>
    <Typography variant="h4">Management Screen</Typography>
  </Container>
);

export default ManagementScreen;
